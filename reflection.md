Reflections -
Lecture 7:
Where I have been asked to use Libaries, I am able to write information and import it into a different file. For example, in students.csv I created a list of students with their IDs, and a set of scores, then in the filehandler.py file I started the code by stating 'import csv'. This is helpful because it saves space in a file and avoids having to write a large amount of code, which could make the program look confusing, and make errors harder to spot effciently.
Another thing I picked up was the "from random import choice", I have used this in my code in the files: main.py, student.csv, and test_grades. For example, with main.py, i have stated 'from filehandler.py import read_student, write_report', which takes the functions 'read_student, write_report' from the file 'filehandler.py' and implements it into the main. By doing this, it allows to use there to be more organistaion, as code is seperated into different files, reuses function (shown in the example), and makes the main.py program shorter and easier to read.
My code also is similar to the "Command Line Arguments" examples, as in main.py, I have used the 'try', and 'expect' functions on line 10 and 23. What this does is the code within 'try' runs normally and if there are any errors, Python moves to the 'except' part of the code which will handle the problem. Although my code isn't exactly the same as the example, it still uses exceptation handling to catch errors and allows the program to keep running.