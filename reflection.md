Where I have been asked to use Libaries, I am able to write information and import it into a different file. For example, in students.csv I created a list of students with their IDs, and a set of scores, then in the filehandler.py file I started the code by stating 'import csv'. We saw this been used in lecture 7. This is helpful because it saves space in a file and avoids having to write a large amount of code, which could make the program look confusing, and make errors harder to spot effciently.
Another thing I picked up from Lecture 7, was the "from random import choice", I have used this in my code in the files: main.py, student.csv, and test_grades. For example, with main.py, i have stated 'from filehandler.py import read_student, write_report', which takes the functions 'read_student, write_report' from the file 'filehandler.py' and implements it into the main. By doing this, it allows to use there to be more organistaion, as code is seperated into different files, reuses function (shown in the example), and makes the main.py program shorter and easier to read.